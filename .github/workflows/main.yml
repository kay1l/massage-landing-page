name: Massage_App

on:
  push:
    branches:
      - main  

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📦 Install dependencies
        run: npm install  

      - name: 🔧 Build static file
        run: npm run build 

      - name: 📁 FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 'ftp.shaishasleisurehub.com'
          username: 'frontend@shaishasleisurehub.com'
          password: 'A}8*f+!IpN#t'
          port: 21
        
